// Creating some basic functionality
const num1 = 5;
const num2 = 10;

// Function to add these values

function addValues () {
    console.log(`The sum is : ${num1 + num2}`);
    
}

// Calling the function, if you don't call it won't show on the screen
addValues()